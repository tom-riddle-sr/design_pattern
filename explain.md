# **創建型設計模式（Creational Design Patterns）**

創建型設計模式是用來幫助我們更方便地創建物件的方式。這些模式可以讓我們的程式更靈活，並且更容易重複使用。

## 常見的創建型設計模式

1. **單例模式（Singleton）**
   - 確保一個類別在整個程式中只有一個實例，並提供一個方法來取得這個實例。
   - 範例：一個程式中只有一個資料庫連線。

2. **工廠方法模式（Factory Method）**
   - 提供一個方法來創建物件，但具體的物件類型由子類別決定。
   - 範例：根據需求創建不同的形狀，例如圓形或方形。

3. **抽象工廠模式（Abstract Factory）**
   - 提供一個方法來創建一組相關的物件，而不需要知道它們的具體類型。
   - 範例：設計一個程式可以同時支援 Windows 和 Mac 的按鈕和文字框。

4. **建造者模式（Builder Pattern）**
   - 將創建一個複雜物件的過程分成多個步驟，讓我們可以一步步地建造出來。
   - 範例：建造一棟房子，可以選擇不同的材料和設計（木屋、玻璃屋）。

5. **原型模式（Prototype Pattern）**
   - 通過複製一個已經存在的物件來創建新物件。
   - 範例：在遊戲中複製一個角色，快速生成多個相似的角色。

---

# **結構型設計模式（Structural Design Patterns）**

結構型設計模式是用來幫助我們更好地組合物件，讓系統的不同部分可以更靈活地協作，並且在系統變化時不需要大幅修改。

## 常見的結構型設計模式

1. **適配器模式（Adapter）**
   - 將一個物件的功能轉換成另一種形式，讓它能被其他系統使用。
   - 範例：將舊版的 API 轉換成新版的格式。

2. **橋接模式（Bridge）**
   - 將物件的功能和實作分開，讓它們可以各自獨立變化。
   - 範例：遙控器可以控制不同的設備，例如電視或音響。

3. **組合模式（Composite）**
   - 將物件組合成樹狀結構，讓我們可以用相同的方式處理單個物件和物件的集合。
   - 範例：檔案系統中的檔案和資料夾。

4. **裝飾者模式（Decorator）**
   - 動態地為物件添加新功能，而不需要改變它的結構。
   - 範例：為一個按鈕添加滾動條或邊框。

5. **外觀模式（Facade）**
   - 提供一個簡單的介面，讓我們可以更方便地使用一組複雜的功能。
   - 範例：家庭劇院的控制面板，可以同時控制電視、音響和燈光。

6. **享元模式（Flyweight）**
   - 透過共享相同的資料，減少記憶體的使用。
   - 範例：在遊戲中渲染森林時，共享樹的類型資料，而不是為每棵樹都創建一個完整的物件。

7. **代理模式（Proxy）**
   - 為其他物件提供一個中介，來控制對該物件的存取。
   - 範例：虛擬代理用於延遲載入圖片，保護代理用於檢查使用者的權限。

---

# **行為型設計模式（Behavioral Design Patterns）**

行為型設計模式專注於物件之間的互動和責任分配，幫助我們更好地管理物件之間的複雜關係。

## 常見的行為型設計模式

1. **策略模式（Strategy Pattern）**
   - 定義一組算法，並將每個算法封裝到一個類中，讓它們可以互相替換。
   - 範例：支付系統中選擇不同的支付方式（信用卡、PayPal）。

2. **觀察者模式（Observer Pattern）**
   - 定義一對多的依賴關係，當一個物件的狀態改變時，所有依賴它的物件都會收到通知並自動更新。
   - 範例：YouTube 頻道通知訂閱者有新影片上傳。

3. **訪問者模式（Visitor Pattern）**
   - 允許你在不改變物件結構的情況下，為物件添加新的操作。
   - 範例：文件系統中，訪問者可以計算文件大小或列印文件名稱。

4. **命令模式（Command Pattern）**
   - 將請求封裝成物件，從而可以參數化請求，並支援撤銷和重做操作。
   - 範例：文字編輯器中的撤銷/重做功能。

5. **模板方法模式（Template Method Pattern）**
   - 定義一個算法的骨架，允許子類別覆蓋特定步驟，而不改變算法的結構。
   - 範例：飲料製作過程（泡茶、煮咖啡）。

6. **狀態模式（State Pattern）**
   - 允許物件在內部狀態改變時改變其行為。
   - 範例：電梯的運行狀態（上升、下降、停止）。

7. **中介者模式（Mediator Pattern）**
   - 通過一個中介者物件來協調多個物件之間的互動，減少物件之間的耦合。
   - 範例：聊天室系統中的訊息中介者。

8. **責任鏈模式（Chain of Responsibility Pattern）**
   - 將請求沿著一條鏈傳遞，直到有物件處理它為止。
   - 範例：技術支援系統中的多層級問題處理。

10. **迭代器模式（Iterator Pattern）**
    - 提供一種方法，依序訪問集合中的每個元素，而不需要暴露其內部結構。
    - 範例：遍歷列表或集合中的元素。

11. **備忘錄模式（Memento Pattern）**
    - 保存物件的狀態，以便在需要時恢復到之前的狀態。
    - 範例：遊戲中的存檔和讀檔功能。

